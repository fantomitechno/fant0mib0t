export type Config = {
    automod: {
        antilink: boolean,
        uppercase: boolean,
        spam: boolean,
        dupplicated: boolean
    },
    antilinkBypass: string,
    linkPreview: boolean
}